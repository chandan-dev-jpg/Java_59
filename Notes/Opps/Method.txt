Method Chaining

Definition:
Method chaining is a technique in which multiple methods are called on the same object in a single statement. It works when each method returns the current object (this) or another object.